# Legacy Voice Call Client

This folder stores deprecated realtime voice-call frontend code.

- `voiceStreamClient.ts`: old WebSocket voice stream client.
- Keep only for fallback/debug when legacy voice call is explicitly enabled.
- Do not use in new chat flows; use async voice-note flow instead.
